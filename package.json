{
  "name": "taskflow",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npx tsc",
    "start": "cd backend && npm install && npx tsc && node dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
